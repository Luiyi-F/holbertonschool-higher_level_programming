#!/usr/bin/node
// print different language with a loop
const array = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let index = 0; index < array.length; index++) {
  console.log(array[index])
}
