#!/usr/bin/python3
word = "Holberton"
word_first_3 = f"{word[0:3]}"               # use slides but take only 3 first letters
word_last_2 = f"{word[7:9]}"                # use slides but take only 2 last letters
middle_word = f"{word[1:8]}"                # use slides but no take the firts adn last letters
print(f"First 3 letters: {word_first_3}")   # output: hol
print(f"Last 2 letters: {word_last_2}")     # output: on
print(f"Middle word: {middle_word}")        # output: olberto
